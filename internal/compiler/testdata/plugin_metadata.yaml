name: plugin metadata distinct keys
left:
  plugin_metadata:
    - id: p1
      foo: bar
right:
  plugin_metadata:
    - id: p2
      enabled: true
expected:
  plugin_metadata:
    - id: p1
      foo: bar
    - id: p2
      enabled: true
