# Validator - Technical

- Execute `apisix test` against the temp config path.
- Capture stdout/stderr for logs and error reporting.
- Return a typed error so the dispatcher can surface failure state.
